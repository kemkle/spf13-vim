" General {

    " Set to auto read when a file is changed from the outside
    set autoread

" }

" Vim UI {

    set nocursorline                  " Do NOT highlight current line as it makes moving(hjkl) rough

    set nonu                        " Line numbers off

" }

" Key (re)Mappings {

    " Fast saving
    nmap <leader>w :w!<cr>

    " These make <Esc>(from insert mode to normal) slow on my linux... :(
    unmap [F
    iunmap [F
    unmap [H
    iunmap [H

" }

" GUI Settings {

    if has('gui_running')
        set guioptions-=m           " Remove the menu
        if has("gui_gtk2")
            set guifont=Monaco\ 11,Menlo\ Regular\ 15,Consolas\ Regular\ 16,Courier\ New\ Regular\ 18
        endif
    endif

" }
